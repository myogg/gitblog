# [当代码跑得比云还快](https://github.com/myogg/gitblog/issues/50)

换了新的TTS接口后，自动生成语音突然失灵了。手动触发却一切正常。

排查了很久，发现原因很简单：新接口太快了。

原来的TTS生成一段语音要几十秒，这段时间里，GitHub的API有足够时间把新文章同步到查询结果中。换上新接口后，两三秒就生成完了，API还没来得及同步，自然找不到新文章。

所以手动触发没问题——因为手动操作本身就耽误了几秒。自动触发就不行了，打标签后立即运行，快过了云服务的同步速度。

你看，代码跑得太快，有时候也是问题。